{
  "name": "telegram-browser-bot-railway",
  "version": "1.0.0",
  "description": "Telegram bot to control a Playwright browser with screenshots and clickable numbered boxes, ready for Railway.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/browserController.js && node --check src/telegramBot.js && node --check src/healthServer.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-telegram-bot-api": "^0.66.0",
    "playwright": "^1.49.1"
  }
}
